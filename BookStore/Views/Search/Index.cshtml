@model IEnumerable<Book>

@foreach (var p in Model)
{
    <div claвs="card card-outline-primary m-1 р-1">
        <div class="bg-faded р-1">
            <h4>
                <a></a>@p.Name
                <span class="badge badge-pill badge-primary" style="float:right">
                    <small>@p.Price.ToString("c")</small>
                </span>
            </h4>
        </div>
        <form id="@p.BookId" asp-action="AddToCart" asp-controller="Cart" method="post">
            <input type="hidden" asp-for="@p.BookId" />
            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
            <span class="card-text p-1">
                @p.Description

                <button type="submit" class="btn btn-success pull-right" style="float:right">Add To Cart</button>
            </span>
        </form>

    </div>

}